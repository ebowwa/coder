# Counter App

React frontend + Bun backend with SQLite.

## Run

```bash
# Terminal 1 - Backend
cd backend && bun run index.ts

# Terminal 2 - Frontend
cd frontend && bunx serve .
```

Open http://localhost:3000
