/**
 * useTerminalSize - Hook for tracking terminal dimensions
 *
 * Re-exports from @ebowwa/tui-core for backwards compatibility.
 * Provides responsive terminal width/height that updates on resize.
 */

export {
  useTerminalSize,
  type TerminalSize,
  type TerminalSizeOptions as UseTerminalSizeOptions,
} from "@ebowwa/tui-core"
